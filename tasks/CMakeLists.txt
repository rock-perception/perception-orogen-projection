# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(projectionTaskLib)
ADD_LIBRARY(${PROJECTION_TASKLIB_NAME} SHARED 
    ${PROJECTION_TASKLIB_SOURCES})
add_dependencies(${PROJECTION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${PROJECTION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${PROJECTION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PROJECTION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PROJECTION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${PROJECTION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PROJECTION_TASKLIB_HEADERS}
    DESTINATION include/orocos/projection)

